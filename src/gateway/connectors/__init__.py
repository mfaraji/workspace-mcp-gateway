"""Connector abstraction: each provider is an independently-connectable unit.

See :mod:`gateway.connectors.base` for the ``Connector`` descriptor and
:mod:`gateway.connectors.registry` for the concrete list this gateway exposes.
"""

from __future__ import annotations
